Implemented [solution.py](/private/tmp/mlx-bench-workspaces/codex_p05_kernel_rowsum_D_s1_r0/workspace/solution.py).

`row_sum(x)` uses a 256-thread custom Metal kernel per row, with guarded strided loads and threadgroup-memory reduction, so arbitrary `N` widths are handled. Python syntax was validated; MLX runtime isn’t installed in this environment for execution testing.