Implemented [solution.py](/private/tmp/mlx-bench-workspaces/codex_p06_mxfp4_quant_B_s1_r2/workspace/solution.py).

It packs weights with `mode="mxfp4"` using MLX’s defaults and performs `x @ w.T` via `mx.quantized_matmul`, returning float32. Syntax compilation passed; MLX itself is not installed in this workspace, so runtime verification wasn’t possible here.