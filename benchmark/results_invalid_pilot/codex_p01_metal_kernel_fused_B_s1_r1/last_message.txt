Created [solution.py](/Users/alpha/asic/mlx-agentic-development/benchmark/results/runs/codex_p01_metal_kernel_fused_B_s1_r1/scratch/workspace/solution.py).

It exposes `fused_scale_bias(x, scale, bias)` using a module-cached `mx.fast.metal_kernel` that performs `x * scale + bias` directly on the GPU, preserving `x`’s shape and dtype. Syntax check passed; runtime MLX verification wasn’t possible because MLX is not installed in this shell.