# Sources

Original condensed writing. Nothing mirrored.

| Page | Upstream |
|---|---|
| `SKILL.md` | [MLX install docs](https://ml-explore.github.io/mlx/build/html/install.html) (MIT); PyPI metadata for `mlx` and `mlx-lm` |

## Verified by execution

Installed `mlx==0.32.0` and `mlx-lm==0.31.3` into a uv-managed Python 3.12 venv on an
Apple M5, macOS 26.5.2, 2026-08-04. `mx.metal.is_available()` True,
`device_info()["architecture"]` `applegpu_g17g`.
